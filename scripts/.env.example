# MonitHQ Infrastructure Monitoring Configuration

# Required: Your MonitHQ site ID (get from site URL)
MONIT_SITE_ID=your-site-id-here

# Required: Your MonitHQ API token (generate from Settings > API Keys)
MONIT_API_TOKEN=your-api-token-here

# Required: MonitHQ API URL
MONIT_API_URL=http://localhost:3000

# Optional: Database configuration for query monitoring
DATABASE_NAME=myapp_production

# Optional: Slow query threshold in milliseconds (default: 1000)
SLOW_QUERY_THRESHOLD=1000

# Optional: Kubernetes namespace to monitor (default: default)
K8S_NAMESPACE=default
